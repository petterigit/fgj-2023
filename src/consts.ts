/**
 * Constants that are required in both the game & the site
 */

export const gameCanvasID = `game-canvas-${Math.random() * 10000}`;
export const gameContainerID = `game-container-${Math.random() * 10000}`;
export const gameStartButtonID = `game-start-button-${Math.random() * 10000}`;
